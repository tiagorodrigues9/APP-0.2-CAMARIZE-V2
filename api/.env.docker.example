MONGO_URL=mongodb+srv://joaokusaka27:Oi2cWcwnYEzBXL7X@joaocluster.t5exvmz.mongodb.net/camarize-dev?retryWrites=true&w=majority&appName=JoaoCluster

# Configurações de Email (Gmail)
EMAIL_USER=camarize.alertas@gmail.com
EMAIL_PASS=zscl altx edtz ojkt

# URL do Frontend
FRONTEND_URL=http://localhost:3000

# Configurações de Monitoramento Automático
ENABLE_AUTO_MONITORING=true
MONITORING_INTERVAL_MINUTES=5

# Configurações de Validação de Email
VALIDATE_EMAIL_ON_REGISTER=false

# Configurações do Servidor
PORT=4000
NODE_ENV=development

# Segurança — obrigatório, a API não inicia sem este valor
# Gere um valor seguro com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=seu_secret_aqui

# Chaves VAPID para Push Notifications
VAPID_PUBLIC_KEY=sua_chave_publica_vapid
VAPID_PRIVATE_KEY=sua_chave_privada_vapid
